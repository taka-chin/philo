#include "philo.h"

bool eat(t_philo *philo)
{

}

bool philo(t_philo *philo)
{
	pthread_mutex_lock(philo->);
		true;
	if(hoge)
	pthread_mutex_lock(philo->);
	return();
}

void *observe(void *arg)
{
	t_philo *philo;

	philo = (t_philo *)arg;
	while(true)
	{
		if()

	}

}
